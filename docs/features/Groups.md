# FEATURE METADATA MAP: GROUPS
*Last Synchronized: 2026-07-09 (Auto-verified against live repository)*

**Change log (2026-07-09):** Implemented the "Group Members directory parity" user story — the Members page (reached via the dashboard's "See more" or the Members tab) now runs on the Alumni Directory engine: same fuzzy search, all 12 directory filters (incl. More Filters modal), list/grid card views, `ham_PaginationUtil` pagination, and connections-first ordering. See Section 7.

**Change log (2026-07-09, notifications session):** Root-caused and resolved both blockers from Section 6 — toggle persistence (fields had *read but not edit* FLS in the portal permission sets, so `stripInaccessible(UPDATABLE)` silently stripped them → no-op update) and zero notification delivery (`HAM_Notification__c.HAM_Source_Type__c` is a **restricted picklist** that had no `Groups` value → every insert died with `INVALID_OR_NULL_FOR_RESTRICTED_PICKLIST`, swallowed by the queueable's try/catch). Both fixed **org-side via Setup UI — repo metadata drift outstanding**. Also fixed notification deep-link routing (`ham_MainCmp`), made bell notification rows clickable (`ham_notificationBell`), and fixed the mobile "View Group" button (`ham_groupsDiscovery`). One open bug: the group-dashboard bell-settings popup won't close on outside click. See Section 8.

**Change log (2026-07-08):** Implemented the full "Group Notifications" user story — post amplification (previously a UI stub), the Send Me/Tagged/Amplified toggle set, and a new @mention feature in both posts and comments. Client confirmed: mentions apply to posts and comments, and the master toggle silences everything. See Section 5.

## 1. Functional UI Behavior

**Entry points:**
- **`ham_groupsCmp`** — small "Alumni Groups" homepage widget listing the user's joined groups (max 10), with a search box and "Discover All" button. Clicking any group or the discover/search actions doesn't navigate itself — it dispatches a `viewgroup` event asking a parent page to route into the full module.
- **`ham_groupsDiscovery`** — the full directory page: "My Groups" / "All Groups" tabs, search, category filter dropdown, paginated group cards, and a "Start a New Alumni Group" request modal.
- **`ham_GroupsOrchestrator`** — invisible router (`isExposed: false`, not Experience-Builder placeable) that toggles between `ham_groupsDiscovery` (no `groupId`) and `ham_groupDashboard` (`groupId` set), based on a single `groupId` prop threaded in from whatever page/Aura wrapper owns the URL.

**Inside a single group (`ham_groupDashboard`):**
- User Action: Click a group card → **System Reaction:** dashboard loads group banner/header, membership button (Join/Leave/Cancel Request depending on status), notification-preference toggles, About/Offer/Guidelines text, and tabs for Discussion, Members, Resources, and (admins only) Group Admin.
- User Action: Click **Join Group** (public) → **System Reaction:** joins immediately (Active). On a **private** group → opens a "Request to Join" modal with an optional message; on submit shows a confirmation modal and the membership becomes Pending until a moderator approves it.
- User Action: Click **Leave Group** → **System Reaction:** opens a modal requiring at least one leave-reason checkbox (with free-text "Other"); confirms as a soft-delete (`Status__c = 'Left'`), preserving the reason(s).
- User Action: Post in the **Discussion** tab (`ham_groupPostComposer`) → **System Reaction:** text posts insert immediately; posts with attachments first create a hidden draft post so `lightning-file-upload` has a record to attach files to, then publish on submit. The feed (`ham_groupFeed`) refreshes via a `postcreated` event and also self-polls (15s → 5min backoff) for new posts from others.
- User Action: Type `@Name` while composing a post or comment → **System Reaction:** an inline autocomplete popover (scoped to active members of the current group) appears; selecting a suggestion inserts `@Full Name` into the text. On submit, each mentioned member who is an opted-in active member (and isn't the author themselves) gets a distinct notification — title is the group name, body is `"{Full Name} mentioned you! in {Group Name}"` (author name is masked as "Someone" if the post is anonymous).
- User Action: Pin/delete/report a post or comment, or moderate content/pending members (`ham_groupAdmin`) → **System Reaction:** admin-only actions gated by `FeatureManagement.checkPermission('Hamilton_Community_Moderator')` or an Active admin-role membership row; moderator "Take Action" sends a warn/delete/block email to the reported user.
- User Action: Admin clicks **Amplify Post** from a post's kebab menu → **System Reaction:** flags the post as amplified (badge shown to all members) and asynchronously notifies every active, opted-in group member — title is the group name, body is `"Check out this post in {Group Name}!"`.
- User Action: Member opens the bell dropdown on the group dashboard → **System Reaction:** sees three toggles — **Send Me Notifications** (master), **Tagged Notifications**, **Amplified post Notifications** — the latter two grey out when the master is off. Turning off "Amplified post Notifications" stops amplify alerts for that group only.
- User Action: Browse/bookmark/amplify a link in **Resources** (`ham_groupResources`) → **System Reaction:** admins can add resources and "amplify" one, which notifies all group members asynchronously via a platform event.
- User Action: Browse/search/filter the **Members** tab (`ham_groupMembers`, also reached via the members-section "See more" icon) → **System Reaction:** a directory-parity Members page — directory search bar (multi-word + nickname fuzzy match), the full directory filter set via `ham_alumniSearchFilterCmp` (incl. More Filters modal and removable filter badges), list/grid toggle rendering through `ham_alumniListDisplayCmp`/`ham_alumniGridDisplayCmp` (same cards and connect/bookmark/block actions as the directory), and `ham_PaginationUtil` pagination (9/page). Default ordering is the caller's connections first (A→Z), then non-connections (A→Z), paginating across that boundary.

## 2. Discovered Execution Stack

**Frontend Layer (LWC):**
| Component | Path | Role |
|---|---|---|
| `ham_GroupsOrchestrator` | `force-app/main/default/lwc/ham_GroupsOrchestrator/` | Router: Discovery vs. Dashboard based on `groupId` |
| `ham_groupsCmp` | `force-app/main/default/lwc/ham_groupsCmp/` | Homepage "My Groups" preview widget |
| `ham_groupsDiscovery` | `force-app/main/default/lwc/ham_groupsDiscovery/` | Full group directory/search page |
| `ham_groupDashboard` | `force-app/main/default/lwc/ham_groupDashboard/` | Single-group shell: banner, membership, tabs, composes all sub-tabs |
| `ham_groupFeed` | `force-app/main/default/lwc/ham_groupFeed/` | Discussion feed: posts, comments, filters, moderation actions, polling |
| `ham_groupPostComposer` | `force-app/main/default/lwc/ham_groupPostComposer/` | New post composer: text, tags, links, file attachments, drafts |
| `ham_groupMembers` | `force-app/main/default/lwc/ham_groupMembers/` | Directory-parity members page: composes `ham_alumniSearchFilterCmp`, `ham_alumniListDisplayCmp`/`ham_alumniGridDisplayCmp`, `ham_PaginationUtil`; wires `getGroupMembersDirectory` |
| `ham_groupResources` | `force-app/main/default/lwc/ham_groupResources/` | Curated resources board + admin curation/amplify |
| `ham_groupAdmin` | `force-app/main/default/lwc/ham_groupAdmin/` | Moderation console: flagged content queue + pending member approvals |

Navigation between all of the above is done entirely via bubbling `CustomEvent`s (`viewgroup`, `navigategroup`, `backtodiscovery`, `postcreated`, `expand`) — **no `NavigationMixin`** is used anywhere in the module. A shared `isOverride` prop threads through every component to swap in a "Kirkland" white-label theme/fallback image.

**Controller/Apex Layer:**
- **`Ham_GroupsController.cls`** → the active controller for the entire module (`without sharing`, manual authorization + `Security.stripInaccessible` for CRUD/FLS). ~37 `@AuraEnabled` methods covering: group listing/search (`getGroups`, `getPicklistValues`), group detail/membership (`getGroupDetail`, `joinGroup`, `leaveGroup`), members (`getGroupMembers`), feed/posts/comments (`getGroupFeed`, `checkForNewPosts`, `createPosts`, `createDraftPost`/`publishDraftPost`/`discardDraftPost`, `updatePost`, `togglePinPost`, `deletePost`, `createComment`, `updateComment`), bookmarks (`bookmarkPost`/`unbookmarkPost`, `bookmarkResource`/`unbookmarkResource`), resources (`getGroupResourceCards`, `addResource`, `amplifyResource`), post amplification (**`amplifyPost`** — new, mirrors `amplifyResource`), mentions (**`searchGroupMembersForMention`** — new, scoped @mention search reusing `PostAuthorWrapper`), moderation (`reportContent`, `getReportedContent`, `resolveReport`, `takeReportAction`, `getPendingMembers`, `approveMember`, `rejectMember`), tags (`searchTags`), and file attach (`attachFilesToPost`). `GroupDetailWrapper`'s notification-preference fields are now `notifyOnTagged`/`notifyOnAmplified` (previously `notifyOnLikes`/`notifyOnComments`/`notifyOnPinned`, which were UI-only and never read by any dispatch logic). `createPosts` accepts `Mentioned_Contact_Ids__c` directly on the incoming sobject (same pattern as `Tags__c`); `publishDraftPost` and `createComment` gained an explicit `mentionedContactIds` parameter since they don't take a full sobject payload from the client.
- **`GroupsController.cls`** → **DEAD CODE.** Single method (`getGroups`, `with sharing`), superseded by `Ham_GroupsController.getGroups`. Confirmed via repo-wide grep: zero LWCs import `@salesforce/apex/GroupsController.*`; its only remaining references are stale Apex-class access grants in profile metadata (e.g. `Admin.profile-meta.xml`).
- **`HAM_AlumniConnectionService.cls`** / **`HAM_AlumniDirectoryController.cls`** → reused (not Groups-specific) by `ham_groupMembers` for connection actions and, since 2026-07-09, for the whole member query pipeline: `Ham_GroupsController.getGroupMembersDirectory` validates the caller's Active membership, splits members into connected/non-connected buckets, then delegates to `HAM_AlumniDirectoryController.getDirectoryData`/`getDirectoryDataCount` via their new optional `scopeContactIds` parameter (a surgical hook — all pre-existing callers pass null and are unaffected). This gives the Members page the directory's exact fuzzy search, filter WHERE-building (`HAM_AlumniFilterHelper`), `HAM_Meta_Data__mdt` card fields, and privacy exclusions. `getGroupMembers` (the old bespoke query, Name-LIKE + 2 filters) is retained but no longer called by the LWC.
- **`HAM_GroupNotificationHandler.cls`** → `processNotifications()`, invoked only by the trigger below; resolves opted-in active `HAM_Group_Member__c` rows → active `User` records → hands off to `HAM_NotificationService.sendNotifications()`.

**Automation/Data Layer:**
- **Trigger:** `HAMGroupNotificationTrigger` — `after insert` on platform event `HAM_Group_Notification__e`, one-line dispatch to `HAM_GroupNotificationHandler.processNotifications(Trigger.new)`. Decouples the write path (new posts, amplified resources) from notification fan-out.
- **Async:** `System.enqueueJob(HAM_NotificationQueueable)` fires from `Ham_GroupsController.dispatchPostNotifications` (new/published posts, plus a separate enqueue for post mentions), `createComment` (comment mentions), `amplifyResource`, and **`amplifyPost`** (new); a synchronous `Messaging.CustomNotification` + `HAM_Notification__c` insert happens directly for moderator post-deletion notices; `Messaging.sendEmail` is used for moderation warn/delete/block actions. `HAM_NotificationQueueable` now has 4 constructor overloads: new-post fan-out (`List<Id> postIds>`), amplify (`Id, Id, String amplifyType`), and **mentions** (`Id sourceRecordId, List<Id> mentionedContactIds, Id authorContactId, String mentionType`) — the latter targets only the specific @mentioned members (not the whole group), excludes self-mentions, masks the author as "Someone" for anonymous posts, and is gated on `Receive_Notifications__c = true AND Notify_on_Tagged__c = true`. Both amplify paths share a `dispatchAmplifyNotifications` helper, filter recipients on `Notify_on_Amplified__c = true` (previously unconditional for resources), and send the exact copy `"Check out this {post|resource} in {Group Name}!"` with the group name as the title. All three dispatch paths share a `resolveGroupAlertTypeId()` helper for the `CustomNotificationType` lookup.
- **Objects touched:** `HAM_Group__c`, `HAM_Group_Member__c` (+ new `Notify_on_Tagged__c`/`Notify_on_Amplified__c` fields), `HAM_Group_Post__c` (+ new `Amplified_On__c`, `Mentioned_Contact_Ids__c` fields; `Is_Amplified__c` already existed and was already read by the feed UI, just never settable), `HAM_Group_Comment__c` (+ new `Mentioned_Contact_Ids__c` field), `HAM_Group_Resource__c`, `HAM_Saved_Post__c`, `HAM_Saved_Resource__c`, `HAM_Reported_Content__c`, `HAM_Group_Notification__e` (platform event), `HAM_Notification__c`, plus `Contact`/`User`/`ContentVersion`/`ContentDocumentLink`/`CustomNotificationType`/`OrgWideEmailAddress` as supporting reads. `Mentioned_Contact_Ids__c` (both objects) stores a comma-separated list of Contact Ids — same lightweight pattern as `Tags__c`, not a junction object, since it's only ever read for notification fan-out.
- **Caching:** `Cache.Session` (Platform Session Cache) holds per-contact active-group-membership lists and a per-group `lastUpdate` timestamp used by the feed's polling mechanism; explicitly invalidated on join/leave/approve/block.
- **Unrelated namesake:** `HAM_AttendeeGroupMigrationBatch.cls` is a one-time ETL batch migrating legacy Cvent "Attendee Group" event-billing records into `HAM_Event_Attendee_Group__c` — a different "Groups" concept (event attendee billing) with no relation to the alumni/member Groups UI above.

## 3. Current Technical Logic Summary
A user reaches Groups either through the `ham_groupsCmp` homepage widget or the `ham_groupsDiscovery` directory page, both of which query `Ham_GroupsController.getGroups` and communicate the chosen group purely via bubbling `viewgroup`/`navigategroup` events (no NavigationMixin) up to `ham_GroupsOrchestrator`, which flips its single `groupId` state to render `ham_groupDashboard` instead of Discovery. The dashboard wires `getGroupDetail` and composes the Discussion (`ham_groupFeed`/`ham_groupPostComposer`), Members (`ham_groupMembers`), Resources (`ham_groupResources`), and Admin (`ham_groupAdmin`) tabs, all of which call further `Ham_GroupsController` `@AuraEnabled` methods imperatively or via `@wire`. Writes (posts, comments, joins, moderation actions) run through manual membership/role checks and `Security.stripInaccessible` since the controller is `without sharing`, then insert/update/delete the relevant `HAM_Group_*` custom objects. Certain writes (new posts, amplified resources) also publish `HAM_Group_Notification__e` platform events, picked up by `HAMGroupNotificationTrigger` → `HAM_GroupNotificationHandler`, which resolves opted-in members to active Users and dispatches push notifications asynchronously — decoupling the UI's write path from notification fan-out.

## 4. Open Items / Follow-ups
- `ham_groupPostComposer`'s `lightning-file-upload` path (`handleUploadFinished`) relies on `ContentDocumentLink.Visibility = 'AllUsers'`; a test comment in `Ham_GroupsControllerTest.cls` implies this may be enforced by a `ContentDocumentLink` trigger/handler (possibly `HAM_ContentDocumentLinkHandler`) as a backstop, separate from the `attachFilesToPost` base64 path. Not yet located/verified — worth tracing if file-attachment behavior changes.
- `GroupsController.cls` is confirmed dead code; candidate for removal along with its stale profile permission grants.
- New `Notify_on_Tagged__c`/`Notify_on_Amplified__c` (on `HAM_Group_Member__c`) and `Amplified_On__c` (on `HAM_Group_Post__c`) fields were granted FLS only via the app-specific permission sets already used by the module (`HAM_Admin`, `HAM_Portal_Access_Internal`, `HAM_Partner_Community_Permission`). The deprecated fields (`Notify_on_Likes__c`/`Notify_on_Comments__c`/`Notify_on_Pinned__c`) additionally have FLS grants scattered across ~50 individual profile-meta.xml files (an org-wide "visible to all profiles" default from however they were originally created) — the two new fields were **not** back-filled into those 50 profiles, since that pattern looks like an artifact of the Setup UI rather than deliberate per-feature access control. Revisit if a profile (rather than a permission set) turns out to gate portal-user access in this org.

## 5. Group Notifications User Story — Implementation Status (2026-07-08)
Full implementation of the "Group Notifications" story (tagged-in-a-post/comment + amplified post/resource alerts, with a 3-toggle bell settings panel). Client confirmed both open scope questions: mentions apply to posts **and** comments, and the master "Send Me Notifications" toggle silences everything for that group (Tagged and Amplified included).

| Requirement | Status |
|---|---|
| Amplified post/resource notification, exact copy `"Check out this post/resource in {Group Name}!"` | **Done.** Both `amplifyPost` (new) and `amplifyResource` (existing) now send this exact body via `HAM_NotificationQueueable.dispatchAmplifyNotifications`, title = group name. |
| Bell dropdown shows only Send Me / Tagged / Amplified toggles | **Done.** `ham_groupDashboard` no longer shows Like/Comment/Pinned toggles. |
| Toggles actually gate notification dispatch | **Done.** Amplified-post/resource and Tagged notifications both filter on `Receive_Notifications__c = true` plus their specific toggle (`Notify_on_Amplified__c` / `Notify_on_Tagged__c`). |
| Tagged in a Post/Comment: `"{Full Name} mentioned you! in [Group Name]"` | **Done.** New @mention popover in `ham_groupPostComposer` (post body) and `ham_groupFeed` (comment box), both detecting an inline `@Name` trigger and searching active group members via `searchGroupMembersForMention`. Mentioned Contact Ids persist to `Mentioned_Contact_Ids__c` on the post/comment; `HAM_NotificationQueueable`'s new mention path notifies each mentioned member individually (excluding self-mentions, masking the author as "Someone" on anonymous posts). |

**Known scope note (not in the original AC, flagged rather than assumed):** pinned-post notifications don't exist in this codebase at all — `togglePinPost` only reorders the feed, it never notifies anyone. A follow-up answer mentioned "the master toggle turns off... amplified and pinned posts," which is read here as an illustrative example of "master silences everything," not a request to build new pin-notification dispatch logic. Flag if that's wrong.

Files touched: `Ham_GroupsController.cls` (+`amplifyPost`, +`searchGroupMembersForMention`, +`parseMentionedContactIds`, +`updateNotificationPreferences`, wrapper field rename, mention wiring in `createPosts`/`publishDraftPost`/`createComment`), `Ham_GroupsControllerTest.cls` (+7 new tests covering amplify-post, all 3 mention scenarios, and the preference-update endpoint), `HAM_NotificationQueueable.cls` (refactored amplify dispatch, added mention dispatch + `resolveGroupAlertTypeId` helper), `ham_groupFeed.js`/`.html`/`.css` (wired the previously-stubbed Amplify Post action; added comment @mention popover), `ham_groupPostComposer.js`/`.html`/`.css` (added post-body @mention popover), `ham_groupDashboard.js`/`.html`/`.css` (toggle swap, custom-toggle UI matching `ham_editProfileCmp`, preference writes moved off Lightning Data Service), `HAM_Group_Member__c` (+2 fields), `HAM_Group_Post__c` (+2 fields), `HAM_Group_Comment__c` (+1 field), plus matching permission-set FLS grants across `HAM_Admin`, `HAM_Portal_Access_Internal`, `HAM_Partner_Community_Permission`.

## 6. Post-testing fixes (2026-07-08, second pass)
Manual testing surfaced several issues after the story above was implemented:

| Issue | Root cause | Fix |
|---|---|---|
| Notification toggles didn't persist after a page refresh | The toggle write went through `lightning/uiRecordApi`'s `updateRecord`, which enforces standard sharing — but `HAM_Group_Member__c` is `ControlledByParent`/private, so a portal user has no standard edit access to their own membership row. Every *other* write in this module deliberately routes through the custom `without sharing` Apex controller for exactly this reason; this one write path was the one place still using LDS directly. | Added `Ham_GroupsController.updateNotificationPreferences(membershipId, currentContactId, changes)` — validates every field in the batch against an allowlist (`Receive_Notifications__c`/`Notify_on_Tagged__c`/`Notify_on_Amplified__c`) and that the membership row belongs to the caller, then updates `without sharing` like the rest of the class. Client reported this fix still didn't resolve persistence after deploying — see below; the client then asked for a Save-button model instead of instant-write, which was implemented and should be retested together. **RESOLVED 2026-07-09:** the Apex rewrite was necessary but insufficient — the *actual* root cause was missing **edit** FLS on the toggle fields in the portal permission sets (read-only grants), which `stripInaccessible(UPDATABLE)` silently stripped. Edit FLS granted org-side; toggles confirmed persisting. See §8. |
| Notification toggles requested to match `ham_editProfileCmp`'s custom toggle switch instead of `lightning-input type="toggle"` | N/A (styling preference) | Replaced the 3 `lightning-input` toggles with the same `.custom-toggle-wrapper`/`.custom-toggle-track` div-based switch used in `ham_editProfileCmp` (including its Kirkland-theme override). |
| Toggles write immediately on tap with no way to review/undo before it's persisted | N/A (UX preference, requested regardless of whether the persistence bug above was the real cause) | Toggles now stage changes locally (`pendingNotificationChanges`) instead of calling Apex on every tap. A Save/Cancel row appears only when there's an unsaved change; Save batches all staged fields into one `updateNotificationPreferences` call, Cancel (or closing the dropdown/backing out without saving) discards them. `receiveNotifications`/`notifyOnTagged`/`notifyOnAmplified` getters now have `effective*` counterparts that show the staged value when present, falling back to the last-saved value otherwise. |
| Composer (`ham_groupPostComposer`) @mention popup shows suggestions, but clicking one doesn't insert `@Name` into the post body | Confirmed via screenshot: the body `<textarea>`'s `value={body}` binding doesn't reliably reflect programmatic changes to `this.body` (the existing `resetForm()` already worked around this with a manual `textarea.value = ''`, which was the tell). | **Fixed.** `handleSelectMentionSuggestion` now explicitly sets `textarea.value = this.body` alongside the focus/cursor-restore logic. |
| Deleting the `@` (or the whole mention query) didn't close the suggestion popup — a stale, already-scheduled search fired later and silently repopulated it | In both `ham_groupPostComposer.detectMentionTrigger` and `ham_groupFeed.handleCommentInput`, `clearTimeout()` on the debounce timer only ran on the "still searching" code path — not on the two early-return branches (no `@` in text, or `@` with nothing typed after it). A search scheduled just before the user deleted back to nothing could still resolve and overwrite the (correctly) cleared suggestion list. | **Fixed** in both files — `clearTimeout()` now runs unconditionally at the top of each function, before any early return. |
| Comment box (`ham_groupFeed`) @mention popup reported as never appearing | `[mention-debug]` console logging (added to investigate) showed the search actually succeeding (`count: 3`) in the client's own test — the debounce-timer bug above is the more likely explanation for what looked like "never shows" (a late stale response could have been clobbering a correctly-populated list back to empty in some sequences). Debug logging removed now that the real bug is fixed. | Considered resolved by the debounce fix above — **please retest and confirm** the dropdown now shows and stays populated correctly. |

## 7. Group Members Directory-Parity User Story — Implementation Status (2026-07-09)

AC: from a group page, "See more" in the members section opens a Members page with the directory's search bar (same fuzzy logic), the same directory filters, list/grid results (connections first A→Z, then non-connections A→Z), and directory-style pagination.

| Requirement | Status |
|---|---|
| "See more" opens the Members page | **Already existed.** `ham_groupDashboard.handleSeeMoreMembers` switches to the Members tab; no navigation change needed. |
| Search bar with the directory's fuzzy search logic | **Done.** The page now queries through the directory engine, inheriting the multi-word AND-per-term match on `ucinn_ascendv2__Expanded_Name_Formula__c` plus the nickname/former-name fallback via `ucinn_ascendv2__Contact_Name__c`. |
| Same filters as the directory tab | **Done.** `ham_groupMembers` embeds `ham_alumniSearchFilterCmp` (all 12 `HAM_Filters__mdt` filters incl. More Filters modal, type-ahead, removable badges); the filter payload feeds the same `HAM_AlumniFilterHelper` WHERE-building. |
| List/grid results, connections first then non-connections, both alphabetical | **Done.** `ham_alumniListDisplayCmp`/`ham_alumniGridDisplayCmp` render the results with the directory's card format and connection actions; `getGroupMembersDirectory` orders the connected bucket first (A→Z each), paginating across the boundary. Card format follows the directory list/grid cards (the AC's "events pages" card is the same row pattern) — flag if the client wanted the events attendee-modal markup instead. |
| Pagination like the directory | **Done.** `ham_PaginationUtil`, 9 records/page (directory parity; the old bespoke pager used 10). |

**Implementation notes:**
- **Scope hook (surgical):** `HAM_AlumniDirectoryController.getDirectoryData`/`getDirectoryDataCount` gained a trailing optional `List<Id> scopeContactIds` parameter that injects `ID IN :scope` into the constituent queries and enables the default (no search/filter) view for scoped calls. All 22 existing test call sites updated with `null`; LWC wire callers unaffected (named params). A guard also disables the CreatedDate keyset skip on the Build-tab decoration query for scoped calls, since those connection rows only decorate the current page's constituents.
- **Directory-parity semantics now apply to members:** the base `ham_AlumniDefaultFilter` WHERE clause and the alumni-privacy exclusion run against members too — i.e. a member with the privacy service indicator is now hidden entirely (the old page showed them masked as "Hidden Member"), and non-alumni contact types are filtered out. Flag if the client wants group members exempt from these directory population rules.
- **Governor profile:** one page render costs up to 2 scoped `getDirectoryDataCount` + up to 2 scoped `getDirectoryData` calls (~30 SOQL worst case on the boundary page) — well within limits but worth knowing before adding more queries to that transaction.
- **Old endpoint:** `Ham_GroupsController.getGroupMembers` is superseded and unused by the UI; removal candidate alongside `GroupsController.cls` (§4).
- **Tests:** `Ham_GroupsControllerTest` adds a self-contained fixture (4 directory-eligible contacts, 1 connection) covering connections-first ordering, cross-bucket pagination, search, Industry filter, and non-member rejection; `HAM_AlumniDirectoryController_Test.testGetDirectoryDataWithContactScope` pins scoped vs null-scope behaviour.

## 8. Group Notifications — Root Causes Solved & Navigation Fixes (2026-07-09, third pass)

Companion detail: [HANDOVER.md](HANDOVER.md) §2–§5. Summary of what was found, fixed, and what remains.

### 8a. Root cause #1 — toggles reverted on Save (Section 6, row 1) — SOLVED
`updateNotificationPreferences` runs `Security.stripInaccessible(AccessType.UPDATABLE, ...)`. The portal permission sets granted the three toggle fields **read but not edit** (`HAM_Partner_Community_Permission`: Tagged/Amplified `editable=false`; `HAM_Portal_Access_Internal`: all three `editable=false`) — so the strip silently removed every field, the `update` was a no-op on a bare Id, no exception was thrown, the LWC's `.then()` cleared staged changes and `refreshApex` repainted the stale value. **Fix:** edit FLS granted org-side by the user; toggles confirmed persisting. The `without sharing` Apex rewrite from §6 was necessary (record access) but orthogonal — sharing ≠ FLS. *Proposed hardening (not implemented): throw `AuraHandledException` if the strip removes any requested field, so FLS regressions fail loudly.*

### 8b. Root cause #2 — no group notifications ever delivered — SOLVED
`HAM_Notification__c.HAM_Source_Type__c` is a **restricted picklist** whose values were only `Connection/News/Event/Campaign`. All four group dispatch paths write `'Groups'` (`HAM_NotificationQueueable` lines ~165/310/439; `Ham_GroupsController` ~1074), so every insert failed `INVALID_OR_NULL_FOR_RESTRICTED_PICKLIST` — swallowed by the queueable's blanket try/catch, leaving `AsyncApexJob` showing *Completed, 0 errors* and the bell empty. Connection notifications always worked because `'Connection'` is a valid value; that asymmetry was the diagnostic key. **Fix:** `Groups` picklist value added org-side by the user; notifications confirmed working. *Proposed hardening (not implemented): stop swallowing DML failures into `System.debug` — this was the second silent-failure bug in this feature.*

### 8c. ⚠️ REPO/ORG DRIFT — critical pending task
Both 8a/8b fixes were made via Setup UI only. The repo still has: `objects/HAM_Notification__c/fields/HAM_Source_Type__c.field-meta.xml` **without** the `Groups` value, and permission-set XML **without** the edit-FLS grants. Deploying either file as-is wipes the fixes. Sync the XML (or retrieve) before any deploy touching them.

### 8d. Notification navigation fixes (local LWC edits, uncommitted)
| Fix | Root cause | File |
|---|---|---|
| Push-tap / deep-link `?view=groups&groupId=...` landed on a dead view | `checkUrlForDeepLink`'s `case 'groups'` activated the **disabled** student-only Groups tab (markup commented out) and read none of `groupId`/`subview`/`postId`. Now routes to Community tab + Groups subtab and threads `groupId`/`subview` — retroactively fixes all stored action URLs. | `ham_MainCmp.js` ~535 |
| Bell dropdown rows weren't clickable at all (`HAM_Action_URL__c` queried but unused) | No handler existed. Rows with an action URL now get `clickable` class + `data-url` + onclick → `window.location.href` (full reload re-enters the deep-link parser — same path as a push tap, desktop + mobile). `stopPropagation()` added to connection Accept/Ignore. Connection rows now navigate too. | `ham_notificationBell` js/html/css |
| Mobile "View Group" button navigation instantly cancelled | `.mobile-card` wrapper had `onclick={handleViewGroup}` but **no `data-id`** — the handler reads `event.currentTarget.dataset.id`, so the bubbled card click dispatched `viewgroup` with `groupId: undefined`, resetting the button's correct navigation. Added `data-id={group.groupId}` (mirrors desktop card). Safe with Show More (already stopPropagations). | `ham_groupsDiscovery.html` ~251 |

### 8e. OPEN BUG — bell-settings popup won't close on outside click (desktop + mobile)
`ham_groupDashboard`'s Notification Settings dropdown has **no backdrop element and no outside-click listener** — only the bell button toggles it (plus programmatic closes in `handleBackAction`). Contrast `ham_notificationBell.html` (~92-94): invisible fixed `div.backdrop` (z-9000 under the z-9999 dropdown) with an onclick that closes — the in-repo pattern. **Proposed fix (not implemented):** replicate the backdrop in `ham_groupDashboard`; its onclick must close the dropdown **and discard `pendingNotificationChanges`** (closing without Save = Cancel, per the Save-button UX contract).

### 8f. Parked, pending client answers
`postId` deep-link (scroll-to-post, ~5 components) · revive student-only Groups tab (needs `isStudent` branch in the parser; mind wire timing) · master-toggle semantics (recommend 4th field `Notify_on_New_Posts__c`, master = pure gate) · mention dedupe (skip generic new-post alert when the member gets the mention alert) · amplified vs pinned feed ordering (recommend Pinned > Amplified > chronological via a third initial-load block; never sort the keyset-paginated query) · un-amplify / re-amplify semantics · new-post copy alignment (title = group name, author masked when anonymous).
